name: CI

on:
  pull_request:
    branches: [develop]
  #push:
  #  branches: [develop]  


jobs:
  test-ci:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v1
      - name: test
        run: echo "Hello World"
