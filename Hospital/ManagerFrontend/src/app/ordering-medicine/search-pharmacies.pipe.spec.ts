import { SearchPharmaciesPipe } from './search-pharmacies.pipe';

describe('SearchPharmaciesPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchPharmaciesPipe();
    expect(pipe).toBeTruthy();
  });
});
